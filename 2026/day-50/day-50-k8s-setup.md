# Day 50 – Kubernetes 架构与集群搭建学习笔记

> [!IMPORTANT]
> **请在此文件中记录你的学习成果，替换所有标记为 `[请在此填写...]` 的区域。**
> 当你修改并保存本文件后，AI 导师会主动帮你进行检查和运行诊断。

---

## 1. Kubernetes (K8s) 的历史与起源
*   Kubernetes 最初是由 **[请在此填写创建者]** 在 **[请在此填写年份]** 年开源的。
*   它的设计深受内部集群管理系统 **[请在此填写灵感系统名称]** 的启发。
*   在希腊语中，Kubernetes 的字面意思是 **[请在此填写含义]**。

---

## 2. Kubernetes 核心架构与组件理解
请用简短的语言或图示描述当你执行 `kubectl apply -f pod.yaml` 时，以下组件是如何协同工作的：

1.  **API Server (kube-apiserver)**：
    *   作用：[请在此填写...]
2.  **etcd**：
    *   作用：[请在此填写...]
3.  **Scheduler (kube-scheduler)**：
    *   作用：[请在此填写...]
4.  **Controller Manager (kube-controller-manager)**：
    *   作用：[请在此填写...]
5.  **kubelet**：
    *   作用：[请在此填写...]
6.  **kube-proxy**：
    *   作用：[请在此填写...]

---

## 3. 本地集群搭建与验证

*   **选择的工具**：[请填入你使用的工具，如 minikube / kind / k3s]
*   **选择原因**：[请在此填写...]

### 集群验证命令输出
请在下方贴出你在终端运行以下命令后的输出内容：

#### 运行 `kubectl get nodes` 输出
```bash
# [请在此贴出输出内容]
```

#### 运行 `kubectl get pods -n kube-system` 输出
```bash
# [请在此贴出输出内容]
```

---

## 4. `kube-system` 核心组件作用分析
在你的 `kube-system` 命名空间下，能够看到哪些运行的 Pod？请至少列举 3 个，并说明它们是做什么的：
1.  **[Pod 名称]**：[作用描述...]
2.  **[Pod 名称]**：[作用描述...]
3.  **[Pod 名称]**：[作用描述...]
